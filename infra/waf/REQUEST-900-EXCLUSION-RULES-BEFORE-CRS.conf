# MICEPP-Tracker CRS exclusions, evaluated before the OWASP Core Rule Set.
#
# No exclusion is currently required. Add only narrowly scoped, documented
# exclusions here after reproducing a false positive in DetectionOnly mode.
# Never disable the rule engine, a complete rule category, or request-body
# inspection to accommodate an application route.
